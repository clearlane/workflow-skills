# A pointer, not a second dependency list.
#
# pyproject.toml is the single source of truth for what these checks need and
# which versions are supported. This file exists because CI caches on a
# requirements file and because `pip install -r requirements.txt` is the
# instruction most contributors reach for first.
#
# Skill guidance stays runtime-neutral; nothing installed here ships to a host
# that loads SKILL.md.
.[dev]
